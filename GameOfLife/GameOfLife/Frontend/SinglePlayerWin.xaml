<Window x:Class="GameOfLife.Frontend.SinglePlayerWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GameOfLife.Frontend"
        mc:Ignorable="d"
        Title="SinglePlayerWin" Height="450" Width="800"
        Closing="SinglePlayerWin_Closing" >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel>
            <Button x:Name="btnStart" Content="Start"  Canvas.Top="55" Click="btnStart_Click" />
            <ComboBox x:Name="cbxMode">
                <ComboBoxItem Content="Selection" HorizontalAlignment="Left" Width="118"/>
                <ComboBoxItem Content="Aleatoire" IsSelected="True" HorizontalAlignment="Left" Width="118"/>
            </ComboBox>
            <Button x:Name="btnBreak" Content="Pause"  Canvas.Bottom="80" IsEnabled="False" Click="btnBreak_Click"/>
            <Button x:Name="btnStop" Content="Stop" IsEnabled="False" Click="btnStop_Click"/>
            <Expander x:Name="expander" Header="Expander" Height="100">
                <Grid Background="#FFE5E5E5">
                    <Button x:Name="button" Content="Button" HorizontalAlignment="Left" Margin="36,28,0,0" VerticalAlignment="Top" Width="75"/>
                </Grid>
            </Expander>
        </StackPanel>

        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" />
        <Canvas  x:Name="GameGrid" Margin="5,0,0,0" Grid.Column="2"/>


    </Grid>
</Window>
